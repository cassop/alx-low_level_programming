#include <stdio.h>
#include <string.h>
/**
 * main - entry point
 *
 * Return: always 0 on success
 */
int main(void)
{
	char hello[52];

	strcpy(hello, "\"Programming is like building a multilingual puzzle");
	puts(hello);
	return (0);
}
